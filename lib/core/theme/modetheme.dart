import 'package:flutter/material.dart';

class modetheme{
static final lightmode=ThemeData(
colorScheme: ColorScheme.fromSeed(seedColor: Color(0xFFFFC886))

);

static final darkmode=ThemeData(
  colorScheme: ColorScheme.fromSeed(seedColor: Color(0xFF5C3B13)),

 
);

}